var bitcoin = require('bitcoinjs-lib')
var bs58 = require("bs58");
var { bytesToHex, hexToBytes } = require("./utils.js");

module.exports = {
  generateKeyPair: () => {    
  },

  generatePrivateKey: () => {    
  },

  privateKeyToWIF: privKey => {
  },

  getPublicKey: privKey => {  
  }
};
